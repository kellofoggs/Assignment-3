My project 3's 4 parts are broken up as follows:

Part 1 read the superblock info from the struct that we fill from a memory map and print it in a format.

Parts 2-4 support subdirectories
Part 2:
	Find the subdirectory entry given the memory mapped file and then print out all of its info. Some bit operations were used to isolate the 3 least significant bits.
	
Part 3:
	Find the desired file and copy it byte by byte to a file outside of the file system. Here the biggest challenge was finding the next block by referring back to the FAT
	
PART 4:


	


